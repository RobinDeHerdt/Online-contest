<?php

use Illuminate\Database\Seeder;

class CreationsTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
     //    $creations = factory(App\Creation::class, 20)->make();

    	// $creations->each(function($creation)
    	// {
    	// 	$user->votes()->save(factory(App\Vote::class)->make());
    	// });
    }
}
